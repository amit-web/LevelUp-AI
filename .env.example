# BUG FIX: this file was missing from the repo, so a fresh clone or a new
# Vercel import had no reference for which env var name the app actually
# reads (app/api/{stream,extras,code}/route.ts all read GROQ_API_KEY).
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=your_key_here
